import React from 'react'
import { Layout } from 'antd'

const { Content } = Layout

const AnimalListCard: React.FC = props => {
    React.useEffect(() => {})

    return (
        <div className="awe-page-card__layout">
            <Layout>
                <Content>
                    <div style={{ height: 600 }}>qwdqwd</div>
                </Content>
            </Layout>
        </div>
    )
}

export default AnimalListCard
